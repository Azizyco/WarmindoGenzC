# 📊 ARCHITECTURE & FLOW DIAGRAM

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      CUSTOMER BROWSER                        │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐            │
│  │   HTML     │  │    CSS     │  │ JavaScript │            │
│  │  (Views)   │  │  (Styles)  │  │  (Logic)   │            │
│  └────────────┘  └────────────┘  └────────────┘            │
│         │               │               │                    │
│         └───────────────┴───────────────┘                    │
│                         │                                     │
│         ┌───────────────▼────────────────┐                  │
│         │  Supabase Client (CDN)         │                  │
│         │  - Database Client             │                  │
│         │  - Storage Client              │                  │
│         │  - Realtime Client             │                  │
│         └───────────────┬────────────────┘                  │
└─────────────────────────┼───────────────────────────────────┘
                          │
                          │ HTTPS / WebSocket
                          │
┌─────────────────────────▼───────────────────────────────────┐
│                     SUPABASE BACKEND                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  PostgreSQL  │  │   Storage    │  │   Realtime   │     │
│  │   Database   │  │   (S3-like)  │  │  (WS Server) │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │     RLS      │  │     Auth     │  │     Edge     │     │
│  │  (Security)  │  │  (Optional)  │  │  Functions   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 User Flow Diagram

### Flow 1: ORDER (Pesan)

```
┌─────────────┐
│   LANDING   │  Customer klik "Pesan"
│   (Home)    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ ORDER START │  Form: nama, kontak, layanan
│             │  If dine-in: pilih meja (RPC)
└──────┬──────┘
       │ sessionStorage.pre_order
       ▼
┌─────────────┐
│    MENU     │  Browse menu, filter, sort
│             │  Chatbot recommendation
│             │  Add to cart (localStorage)
└──────┬──────┘
       │ Cart (3 items)
       ▼
┌─────────────┐
│  CHECKOUT   │  Review cart & customer info
│             │  Choose payment method
│             │  Submit order
└──────┬──────┘
       │ INSERT orders + order_items
       │ Trigger: payment_code, queue_no
       ▼
┌─────────────┐
│   RECEIPT   │  Show payment code (WMG-XXX)
│             │  Show queue number (#1)
│             │  Actions: Pay, Print, Share
└─────────────┘
```

### Flow 2: PAYMENT (Bayar)

```
┌─────────────┐
│   LANDING   │  Customer klik "Bayar Pesanan"
│   (Home)    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│     PAY     │  Input payment code
│             │  Search order by code
└──────┬──────┘
       │ SELECT orders WHERE payment_code
       ▼
┌─────────────┐
│  ORDER      │  Display order details
│  DETAILS    │  
└──────┬──────┘
       │
       ├─────────────┬─────────────┐
       │             │             │
       ▼             ▼             ▼
  ┌────────┐   ┌────────┐   ┌────────┐
  │  CASH  │   │  QRIS  │   │TRANSFER│
  │        │   │        │   │        │
  │ Pay at │   │ Upload │   │ Upload │
  │ Cashier│   │ Proof  │   │ Proof  │
  └────────┘   └───┬────┘   └───┬────┘
                   │            │
                   └────┬───────┘
                        │ INSERT payments
                        │ UPLOAD to storage
                        ▼
                  ┌──────────┐
                  │ PENDING  │
                  │ (waiting │
                  │  verify) │
                  └──────────┘
```

### Flow 3: QUEUE (Antrian)

```
┌─────────────┐
│   LANDING   │  Customer klik "Lihat Antrian"
│   (Home)    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│    QUEUE    │  SELECT vw_queue_today
│   (Board)   │  
└──────┬──────┘
       │
       │ Subscribe to Realtime
       ▼
┌─────────────────────────────────┐
│  REALTIME LISTENER              │
│                                 │
│  ┌──────────────────────────┐  │
│  │ ON: orders.* (any change)│  │
│  │ DO: Reload queue data    │  │
│  └──────────────────────────┘  │
│                                 │
│  ┌──────────────────────────┐  │
│  │ ON: payments.* (any)     │  │
│  │ DO: Reload queue data    │  │
│  └──────────────────────────┘  │
└─────────────────────────────────┘
       │
       │ Auto-refresh every 30s
       ▼
    [Updated Queue Display]
```

---

## 🗄️ Database Schema

```
┌──────────────────────────────────────────────────────────┐
│                        ORDERS                             │
├──────────────────────────────────────────────────────────┤
│ id (uuid, PK)                                            │
│ source (text) = 'web'                                    │
│ guest_name (text) ← from pre_order                       │
│ contact (text) ← from pre_order                          │
│ service_type (text) ← 'dine_in' | 'takeaway'            │
│ table_no (text) ← from RPC get_free_tables               │
│ status (text) ← 'placed' → 'paid' → 'confirmed' ...     │
│ payment_method (text) ← 'cash' | 'qris' | 'transfer'    │
│ payment_code (text, unique) ← AUTO via trigger           │
│ queue_no (int) ← AUTO via trigger (daily)                │
│ total_amount (numeric) ← from cart                       │
│ active (boolean)                                         │
│ created_at (timestamp)                                   │
└──────────────────────────────────────────────────────────┘
       │
       │ 1:N
       ▼
┌──────────────────────────────────────────────────────────┐
│                     ORDER_ITEMS                          │
├──────────────────────────────────────────────────────────┤
│ id (uuid, PK)                                            │
│ order_id (uuid, FK) → orders.id                          │
│ menu_id (uuid, FK) → menus.id                            │
│ quantity (int) ← from cart                               │
│ price (numeric) ← from menus.price                       │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                      PAYMENTS                            │
├──────────────────────────────────────────────────────────┤
│ id (uuid, PK)                                            │
│ order_id (uuid, FK) → orders.id                          │
│ amount (numeric)                                         │
│ method (text) ← 'cash' | 'qris' | 'transfer'            │
│ status (text) ← 'pending' | 'success' | 'failed'        │
│ proof_image_url (text) ← storage path                    │
│ created_at (timestamp)                                   │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                        MENUS                             │
├──────────────────────────────────────────────────────────┤
│ id (uuid, PK)                                            │
│ category_id (uuid, FK) → categories.id                   │
│ name (text)                                              │
│ description (text)                                       │
│ price (numeric)                                          │
│ image_url (text)                                         │
│ is_active (boolean)                                      │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│                     CATEGORIES                           │
├──────────────────────────────────────────────────────────┤
│ id (uuid, PK)                                            │
│ name (text)                                              │
│ description (text)                                       │
│ is_active (boolean)                                      │
└──────────────────────────────────────────────────────────┘
```

---

## 🔐 Security Flow (RLS)

```
┌──────────────────┐
│  ANONYMOUS USER  │  (No login required)
└────────┬─────────┘
         │
         ▼
┌─────────────────────────────────────────────────┐
│              ROW LEVEL SECURITY                 │
├─────────────────────────────────────────────────┤
│                                                 │
│  ORDERS:                                        │
│  ✓ INSERT if source='web'                      │
│  ✓ SELECT if source='web'                      │
│  ✗ UPDATE (admin only)                         │
│  ✗ DELETE (admin only)                         │
│                                                 │
│  ORDER_ITEMS:                                   │
│  ✓ INSERT (any)                                │
│  ✓ SELECT (any)                                │
│  ✗ UPDATE/DELETE                               │
│                                                 │
│  PAYMENTS:                                      │
│  ✓ INSERT (any)                                │
│  ✓ SELECT (any)                                │
│  ✗ UPDATE (admin only)                         │
│                                                 │
│  STORAGE (payment-proofs):                     │
│  ✓ INSERT/UPLOAD (any)                         │
│  ✓ SELECT (own files)                          │
│  ✗ DELETE (admin only)                         │
└─────────────────────────────────────────────────┘
```

---

## 📦 Data Flow

### 1. Pre-Order Data (SessionStorage)
```javascript
sessionStorage.pre_order = {
  guest_name: "John Doe",
  contact: "081234567890",
  service_type: "dine_in",
  table_no: "5"
}
```

### 2. Cart Data (LocalStorage)
```javascript
localStorage.cart = [
  { menu_id: "uuid1", name: "Indomie", price: 15000, quantity: 2 },
  { menu_id: "uuid2", name: "Es Teh", price: 5000, quantity: 1 }
]
```

### 3. Order Creation Flow
```
JavaScript (checkout.js)
    │
    ├─► Calculate total from cart
    │
    ├─► INSERT orders
    │       │
    │       └─► TRIGGER: set payment_code (WMG-XXXXXX)
    │       └─► TRIGGER: set queue_no (daily counter)
    │
    ├─► Get order.id from response
    │
    ├─► INSERT order_items (batch)
    │
    ├─► Clear localStorage.cart
    ├─► Clear sessionStorage.pre_order
    │
    └─► Redirect to receipt.html?id={order.id}
```

---

## 🔄 Realtime Flow

```
┌───────────────────┐
│  QUEUE PAGE       │  Subscribe to 'queue-updates' channel
│  (Customer View)  │
└─────────┬─────────┘
          │
          ▼
┌─────────────────────────────────────────────┐
│     SUPABASE REALTIME CHANNEL               │
│                                             │
│  Listen: orders.* (all changes)            │
│  Listen: payments.* (all changes)          │
└─────────────────────────────────────────────┘
          │
          │ WebSocket Connection
          │
┌─────────▼─────────────────────────────────┐
│  EVENTS FROM ADMIN PANEL:                 │
│                                           │
│  • Admin changes order status             │
│  • Admin confirms payment                 │
│  • Admin cancels order                    │
│  • New order created                      │
└───────────────────────────────────────────┘
          │
          ▼
┌───────────────────┐
│  AUTO-REFRESH     │  Reload queue data
│  (< 2 seconds)    │  Update UI instantly
└───────────────────┘
```

---

## 🎯 Component Interaction

```
┌──────────────────────────────────────────────────────┐
│                   SHARED COMPONENTS                   │
├──────────────────────────────────────────────────────┤
│                                                       │
│  supabase.js                                         │
│  ├─ createClient()                                   │
│  └─ checkConnection()                                │
│                                                       │
│  ui.js                                               │
│  ├─ showToast(type, message)                         │
│  ├─ rp(amount)        ← Format Rupiah                │
│  ├─ showLoading()                                    │
│  ├─ hideLoading()                                    │
│  ├─ getStatusBadge(status)                           │
│  ├─ initOfflineIndicator()                           │
│  └─ handleSupabaseError(error, retryFn)              │
└──────────────────────────────────────────────────────┘
                       │
                       │ Import & Use
                       ▼
┌──────────────────────────────────────────────────────┐
│                   PAGE COMPONENTS                     │
├──────────────────────────────────────────────────────┤
│                                                       │
│  order-start.js                                      │
│  ├─ loadFreeTables()        ← RPC call              │
│  ├─ validateForm()                                   │
│  └─ saveToSession()                                  │
│                                                       │
│  menu.js                                             │
│  ├─ loadMenus()             ← SELECT menus           │
│  ├─ loadCategories()        ← SELECT categories      │
│  ├─ renderMenus()                                    │
│  ├─ filterByCategory()                               │
│  ├─ sortMenus()                                      │
│  ├─ addToCart()             ← localStorage          │
│  └─ showChatbotReco()       ← Rule-based AI         │
│                                                       │
│  checkout.js                                         │
│  ├─ renderCart()            ← From localStorage      │
│  ├─ updateQuantity()                                 │
│  ├─ removeItem()                                     │
│  └─ submitOrder()           ← INSERT orders          │
│                                                       │
│  receipt.js                                          │
│  ├─ loadOrder()             ← SELECT by id           │
│  ├─ renderReceipt()                                  │
│  ├─ printReceipt()          ← window.print()        │
│  └─ shareWhatsApp()         ← wa.me link            │
│                                                       │
│  pay.js                                              │
│  ├─ searchByCode()          ← SELECT by code         │
│  ├─ uploadProof()           ← Storage upload         │
│  └─ submitPayment()         ← INSERT payments        │
│                                                       │
│  queue.js                                            │
│  ├─ loadQueue()             ← SELECT vw_queue_today  │
│  ├─ setupRealtime()         ← Subscribe channel      │
│  └─ renderQueue()           ← Update UI              │
└──────────────────────────────────────────────────────┘
```

---

## 🧩 State Management

```
┌─────────────────────────────────────────────────┐
│              CLIENT-SIDE STATE                   │
├─────────────────────────────────────────────────┤
│                                                  │
│  SessionStorage (temporary, single session)     │
│  └─ pre_order: { name, contact, service, table }│
│     ↳ Cleared after order submitted             │
│                                                  │
│  LocalStorage (persistent)                      │
│  └─ cart: [{ menu_id, name, price, qty }, ...]  │
│     ↳ Cleared after order submitted             │
│                                                  │
│  Memory (page scope)                            │
│  ├─ allMenus: []        ← From database         │
│  ├─ categories: []      ← From database         │
│  ├─ currentOrder: {}    ← From database         │
│  └─ realtimeChannel: {} ← Supabase subscription │
└─────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
┌──────────────────────────────────────────┐
│         STATIC HOSTING                   │
│  (Netlify / Vercel / GitHub Pages)       │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │  /customer/                        │ │
│  │  ├─ index.html      (Landing)     │ │
│  │  ├─ order-start.html              │ │
│  │  ├─ menu.html                     │ │
│  │  ├─ checkout.html                 │ │
│  │  ├─ receipt.html                  │ │
│  │  ├─ pay.html                      │ │
│  │  ├─ queue.html                    │ │
│  │  └─ /js/*.js                      │ │
│  │                                    │ │
│  │  /shared/                          │ │
│  │  ├─ /css/base.css                 │ │
│  │  └─ /js/supabase.js, ui.js        │ │
│  └────────────────────────────────────┘ │
└──────────────┬───────────────────────────┘
               │ HTTPS
               ▼
     ┌─────────────────────┐
     │   CDN JSDELIVR      │
     │   (Supabase JS)     │
     └─────────────────────┘
               │
               ▼
     ┌─────────────────────┐
     │   SUPABASE CLOUD    │
     │   - PostgreSQL      │
     │   - Storage (S3)    │
     │   - Realtime (WS)   │
     └─────────────────────┘
```

---

## 📈 Performance Optimization

```
FIRST LOAD:
  1. HTML           (~3 KB)
  2. CSS            (~15 KB)
  3. Supabase CDN   (~50 KB gzip)
  4. App JS         (~5 KB)
  ────────────────────────────
  TOTAL: ~73 KB

SUBSEQUENT PAGES:
  - HTML only (~3-5 KB)
  - CSS & JS cached
  - Fast navigation

DATABASE QUERIES:
  - Indexed by: id, payment_code
  - RLS optimized
  - View pre-computed
  - Response: < 500ms

REALTIME:
  - WebSocket persistent
  - Auto-reconnect
  - Bandwidth: minimal
```

---

## 🔒 Security Layers

```
┌─────────────────────────────────────────┐
│     1. NETWORK SECURITY                 │
│        - HTTPS only                     │
│        - CORS configured                │
└─────────────────────────────────────────┘
              ▼
┌─────────────────────────────────────────┐
│     2. SUPABASE AUTH                    │
│        - Anonymous access               │
│        - API key validation             │
└─────────────────────────────────────────┘
              ▼
┌─────────────────────────────────────────┐
│     3. ROW LEVEL SECURITY               │
│        - Policy per table               │
│        - Role-based (anon vs auth)      │
└─────────────────────────────────────────┘
              ▼
┌─────────────────────────────────────────┐
│     4. INPUT VALIDATION                 │
│        - Client-side checks             │
│        - Type validation                │
│        - SQL injection (prevented)      │
└─────────────────────────────────────────┘
              ▼
┌─────────────────────────────────────────┐
│     5. STORAGE SECURITY                 │
│        - Private bucket                 │
│        - Signed URLs                    │
│        - File size limits               │
└─────────────────────────────────────────┘
```

---

**Diagram ini membantu memahami:**
- ✅ Struktur sistem secara keseluruhan
- ✅ Alur data dari user ke database
- ✅ Interaksi antar komponen
- ✅ Keamanan berlapis
- ✅ Optimasi performa

